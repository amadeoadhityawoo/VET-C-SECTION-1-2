# RFC-0006 - Codex Implementation Prompt Pack

Status: Draft
Section: Theory RFC
Version: v0.1

## Purpose

Defines the intended prompt-pack workflow for implementing VET-C RFCs incrementally.

## Scope

Includes implementation ordering, guardrails, test expectations, and instructions for avoiding future-scope work too early.

## Current MVP Status

This is currently a theory/specification placeholder. The repository was built incrementally from RFC-1000 through RFC-1015.

## Implementation Notes

The resulting implementation lives under `vetc/`, `tests/`, and `scripts/`.

## Limitations

The prompt pack itself is not fully captured as executable documentation.

## Future Work

Add prompt templates and acceptance criteria for Section 3+ tasks.

